# Frontend environment template — Factory Detective
#
# The dev server proxies /api to the FastAPI backend (default http://127.0.0.1:8000).
# No keys belong here: LLM/vision credentials stay server-side in backend/.env.

# Port of the FastAPI backend the Vite proxy targets (must match vite.config.ts)
VITE_API_PORT=8000
